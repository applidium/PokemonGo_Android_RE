package crittercism.android;

public final class dy extends Thread {
    public dy(Runnable runnable) {
        super(new de(runnable));
    }

    public dy(Runnable runnable, String str) {
        super(new de(runnable), str);
    }
}
